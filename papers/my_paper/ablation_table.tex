% Requires in preamble:
%   \usepackage{booktabs}
%   \usepackage[table]{xcolor}
%   \usepackage{threeparttable}
%   \usepackage{multirow}

\definecolor{rowgreen}{HTML}{C8E6C9}
\definecolor{roworange}{HTML}{FFE0B2}

% ------------------------------------------------------------------
%  TABLE: Black-box occlusion ablation study results
% ------------------------------------------------------------------
%
%  Setup: a 50 % side-length black square (25 % area) is applied to
%  the centre of each test image before inference.  The _best
%  checkpoint from each of the 5 cross-validation folds is evaluated;
%  mean ± std across these 5 checkpoints is reported.
%
%  Conditions tested:
%    C5 (Guha replication) — Preprocessed | Paper labels | Pre-split
%    C3 (Fully corrected)  — Raw          | Corrected labels | Post-split
% ------------------------------------------------------------------

\begin{table}[htbp]
\centering
\caption{Black-box occlusion ablation study: classification performance
         when 25\% of the image area (centre square) is masked.
         Each condition is evaluated on the five best-epoch checkpoints
         from cross-validation; values are mean $\pm$ std.}
\label{tab:ablation}
\begin{threeparttable}
\begin{tabular}{llcccc}
  \toprule
  \textbf{Condition} & \textbf{Model} & \textbf{Accuracy} & \textbf{Precision}
  & \textbf{Recall} & \textbf{F1} \\
  \midrule
  \rowcolor{roworange}
  \multirow{2}{*}{C5 — Preprocessed | Paper | Pre-split}
    & ResNet101   & $.877{\pm}.007$ & $.881{\pm}.020$ & $.877{\pm}.007$ & $.878{\pm}.014$ \\
  \rowcolor{roworange}
    & DenseNet201 & $.797{\pm}.093$ & $.833{\pm}.025$ & $.797{\pm}.093$ & $.799{\pm}.059$ \\
  \addlinespace
  \rowcolor{rowgreen}
  \multirow{2}{*}{C3 — Raw | Corrected | Post-split}
    & ResNet101   & $.868{\pm}.006$ & $.826{\pm}.017$ & $.868{\pm}.006$ & $.827{\pm}.012$ \\
  \rowcolor{rowgreen}
    & DenseNet201 & $.686{\pm}.268$ & $.755{\pm}.041$ & $.686{\pm}.268$ & $.670{\pm}.242$ \\
  \bottomrule
\end{tabular}
\begin{tablenotes}
  \footnotesize
  \item[*] Occlusion box side = 50\% of \(\min(h, w)\), applied to the
           image centre. Accuracy $=$ Recall under weighted averaging.
  \item[$\dagger$] The high variance in DenseNet201/C3 reflects one
           fold collapsing to near-chance performance (fold~3:
           accuracy $= 0.221$), suggesting instability under the
           corrected, balanced protocol.
\end{tablenotes}
\end{threeparttable}
\end{table}
